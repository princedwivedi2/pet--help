# Chat mode: 'mock' or 'api'
# mock: Uses local rule-based mock responses
# api: Uses the serverless API endpoint
VITE_CHAT_MODE=mock

# Add your API keys here (in production, these should be securely stored)
# OPENAI_API_KEY=your_key_here
